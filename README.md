# anduril-sdk-rust

```crate
[package]
name = "hello-rust"
version = "0.1.0"
edition = "2021"

[dependencies]
anduril-sdk = { git = "https://github.com/anduril/anduril-rust", branch = "main" }
```

```bash
crate fetch
```
